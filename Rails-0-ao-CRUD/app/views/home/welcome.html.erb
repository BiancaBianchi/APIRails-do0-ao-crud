<h1>Boas vindas a Task List!</h1>
<p>Sua lista de tarefas:</p>

<% @tasks.each do |task| %>
  <%= task.description %>
  <% link_to "Editar", edit_task_path(task) %>
  <% link_to "Apagar", task_path(task), method: :delete, 
  data: {confirm:  'Quer apagar essa tarefa?' } %>
<% end %>
